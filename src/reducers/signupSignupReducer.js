const initialState = {
  loading: false,
  registerError: '',
  loginError: '',
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
