const initialState = {
  loading: false,
  registerError: '',
  loginError: '',
};

const signupSignin = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default signupSignin;
